# Rollout engine (MLX, Apple Silicon GPU) + analysis stack.
# Install into a homebrew-python venv (see README). No API keys needed: the dataset
# generator and the strong-model judge call the local `claude` CLI in headless mode.
mlx-lm==0.31.3
numpy==2.4.6
scipy==1.17.1
pandas==3.0.3
matplotlib==3.10.9
huggingface_hub==1.18.0
