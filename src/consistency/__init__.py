"""Consistency-training preliminary test: is the consistency reward equivalent to a
stronger reward model? Pipeline: generate -> rollout -> reward -> judge -> correlate.

See control-room/preliminarytesting.md and control-room/ExperimentDesign.md.
"""
