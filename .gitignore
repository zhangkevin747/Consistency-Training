# python
.venv/
__pycache__/
*.pyc

# data + results: NEVER track. Question sets derived from the acorn consistency
# dataset (shared privately, password-protected) must not leak into the repo.
data/
results/

# secrets
.env

# os / tmp
.DS_Store
/tmp/

# the acorn consistency dataset itself (private; do not commit in any form)
acorn-dataset/
acorn*.zip
